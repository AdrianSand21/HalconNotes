<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HalconNotes">
        
        <!-- Nueva SplashActivity como LAUNCHER -->
        <activity
            android:name=".presentacion.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar"> <!-- Aseguramos NoActionBar para Splash -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".presentacion.GraficaActivity" />

        <activity
            android:name=".presentacion.PromedioActivity"
            android:exported="true" />

        <activity android:name=".presentacion.NotasActivity" />
        
        <!-- MainActivity ya no es LAUNCHER, solo DEFAULT -->
        <activity
            android:name=".presentacion.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" /> <!-- Cambio importante -->
            </intent-filter>
        </activity>

    </application>


</manifest>